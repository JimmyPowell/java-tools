# 工具类说明

本仓库包含若干常用的 Spring Boot 工具类，位于 `com.example.tools` 包下。

## UuidUtils

- `randomUuid()`：生成带 `-` 的版本 4 UUID。
- `randomUuidWithoutDash()`：生成不带 `-` 的版本 4 UUID。

## CaptchaUtils

- `numericCode(int length)`：生成指定长度的数字验证码。

## JwtUtils

- 读取 `application.yml` 中配置的 `jwt.privateKey` 作为 HMAC-SHA256 签名密钥。
- `generateAccessToken(String userUuid, String identity, long expireMillis)`：生成访问令牌。
- `generateRefreshToken(String userUuid, String identity, long expireMillis, int version)`：生成带版本号的刷新令牌。
- `refreshToken(String refreshToken, long newExpireMillis)`：刷新刷新令牌的过期时间。
- `encode(Map<String, Object> payload)`：自定义简易 JSON 序列化生成 JWT。
- `decode(String token)`：解析 JWT 并返回负载信息。

此实现仅用于演示，未涵盖完整的 JWT 功能。

## OssUtils

占位类，后续可补充对象存储相关操作。

## MailUtils

占位类，后续可补充邮件发送相关操作。

